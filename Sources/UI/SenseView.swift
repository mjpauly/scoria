import Foundation
import SwiftUI

struct SenseView: View {
  
    var body: some View {
        VStack {
            LocationConfigView()
        }
    }
}

struct SenseView_Previews: PreviewProvider {
    static var previews: some View {
        SenseView()
    }
}
